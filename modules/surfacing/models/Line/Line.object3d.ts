import {EntityObject3D} from '../../three';

/**
 * Line segments are drawn by Cage.object3d as part of the grid; an individual
 * Line entity has no standalone visual. Stub kept to preserve the uniform
 * EntityObject3D API across every entity type.
 */
export class LineObject3D extends EntityObject3D {
}
